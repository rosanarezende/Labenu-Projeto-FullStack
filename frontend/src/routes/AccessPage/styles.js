import styled from "styled-components"

export const AcessWrapper = styled.div`
    margin: 0;
    padding: 0;
    height: 100vh;
    width: 100vw;
    display: flex;
    align-items: center;
    justify-content: center;
    background-image: linear-gradient(rgba(30, 50, 100, 1), rgba(44, 94, 146, 1));
`

export const AcessLogo = styled.img`
    width: 200px;
`